# Secrets for authentication using CHAP
# client  server  secret      IP addresses
<% node[:pptpd][:users].each do |user| %>
  <%= user[:username] %> pptpd <%= user[:password] %> <%= user[:ip_address] || "*" %>
<% end %>
